# Streamlit Community Cloud 배포용.
# app.py 는 표준 라이브러리와 streamlit 만 쓰고, data/cases.json 만 읽는다.
streamlit>=1.36

# 아래는 build_data.py (DART 원문 수집/추출) 전용이며 앱 실행에는 필요 없다.
# 배포 환경에 설치하면 빌드만 느려지므로 주석 처리한다.
# 로컬에서 데이터를 다시 만들 때만 설치:  pip install lxml requests python-dotenv
# lxml>=5.2
# requests>=2.31
# python-dotenv>=1.0
